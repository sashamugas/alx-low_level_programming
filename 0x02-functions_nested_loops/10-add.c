#include "main.h"
/**
 *add - add 2 integers
 *add: a function that adds 2 integers and returns the results
 *@a: first number
 *@b: second number
 *
 * Return: result of addition of @a and @b
 */
int add(int a, int b)
{
	return (a + b);
}
